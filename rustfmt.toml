# Import grouping and per-crate merging are currently unstable rustfmt options.
# Run `nix run .#fmt` or `nix run .#rustfmt`; the flake wrapper selects the
# pinned nightly toolchain before invoking `cargo fmt`.
group_imports = "StdExternalCrate"
imports_granularity = "Crate"
